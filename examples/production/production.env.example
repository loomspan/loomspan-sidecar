# Copy to production.env and protect it from other users. Replace every example value.
SIDECAR_IMAGE=loomspan-sidecar:sc5-local
HTTPS_BIND_ADDRESS=127.0.0.1
HTTPS_PORT=8443
HTTP_BIND_ADDRESS=127.0.0.1
HTTP_PORT=8080
SITE_HOST=sidecar.home.arpa
CADDY_CONFIG_FILE=./Caddyfile.internal
TLS_DIR=./secrets/tls
# For DNS mode, build Dockerfile.caddy-dns and set CADDY_IMAGE to that tag.
# CADDY_IMAGE=loomspan-caddy-cloudflare:local
# CF_API_TOKEN=store-in-protected-production-env-only
JWT_PUBLIC_KEY_FILE=./secrets/jwt-public.pem
JWT_ISSUER_URI=https://issuer.example.test
JWT_AUDIENCE=loomspan-sidecar
MODEL_DRIVER=openai
MODEL_BASE_URL=https://model.example.test/v1
MODEL_API_KEY=replace-with-provider-key
MODEL_NAME=replace-with-provider-model
SETUP_TOKEN=replace-with-one-time-random-credential
SMTP_FROM=sidecar@example.test
EXTERNAL_BASE_URL=https://sidecar.home.arpa:8443
SMTP_HOST=smtp.example.test
SMTP_PORT=587
SMTP_USERNAME=replace-with-smtp-user
SMTP_PASSWORD=replace-with-smtp-password
SMTP_AUTH=true
SMTP_STARTTLS=true
# Optional: names are an explicit allowlist for authored REST base-url references.
URL_VARIABLES=TARGET_URL
TARGET_URL=https://api.example.test
