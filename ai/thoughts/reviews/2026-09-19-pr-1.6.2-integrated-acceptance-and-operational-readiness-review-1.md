# PR 1.6.2 Integrated Acceptance and Operational Readiness Code Review — Cycle 1

## Scope and Repository State

Reviewed the ticket-scoped unstaged changes to `README.md`, the ticket, the production guide, `scripts/verify-production.py`, and `ProductionComposeBrowserIntegrationTest`, plus the untracked research and plan artifacts. Branch `main` was at `08488f27131dd4e851d2b611b9d1251f0fe9fb89`; there were no staged changes. I traced the verifier through the production Compose file, management/API services, bundle format, fixture host, database recovery, and shutdown scripts. The review fix also changed the test-only host fixture and implementation-plan record. No production Java code or release dependency was changed.

## Findings

### [P2] Prove the destination URL binding rather than only callback success

- Location: `scripts/verify-production.py:500` (pre-fix callback assertion)
- Scenario: The destination Compose host has both `host` and `fixture-destination` network aliases. If an imported REST route resolved `${TARGET_URL}` to `http://host:8081` rather than the destination's configured `http://fixture-destination:8081`, execution still reached the same fixture container and the existing path assertion passed.
- Impact: The integrated verifier could claim destination-specific URL resolution without testing it, leaving a primary transfer acceptance condition unproved. Its ZIP inspection also counted skill files without comparing the imported authored YAML and route text to the source.
- Evidence: The destination override in `scripts/verify-production.py` adds `fixture-destination` to the existing `host` service; the former assertion checked only the callback path in that host's `/status` response. The fixture did not record the callback Host header.
- Fix: The test-only fixture now records the callback Host header. The verifier asserts `fixture-destination:8081` and compares the complete imported authored configuration with the source configuration.

## Findings Resolved in This Context

- Resolved the P2 transfer-evidence gap in `examples/quickstart/host/host.py:128` and `scripts/verify-production.py:387,500-504`. Updated the plan's implementation record to reflect the fixture change. Re-reviewed the changed test flow and reran the complete disposable deployment verifier successfully.

## Acceptance-Criteria and Plan Conformance

| Criterion/decision | Code evidence | Test evidence | Result |
| --- | --- | --- | --- |
| Mail, HTTPS, setup, invitation, recovery, outage, lock | Production verifier and Chromium password-link test; test-only SMTP override | Fresh-volume, email, browser, outage and JWT checkpoints in full Compose rerun | implemented |
| Author/publish, REST/model execution, editing privacy/staleness, roles/CSRF, accepted update | Verifier, concurrent Chromium test; existing management HTTP tests for disconnect/logout | Browser publish and private-draft checkpoints; focused Sidecar test run | implemented |
| Two-instance transfer, destination binding, invalid-state preservation, import and rollback | Separate Compose projects/volumes; bundle review/confirm; Host capture and exact authored-config comparison | Destination, rejection, fresh-ID and rollback checkpoints in full Compose rerun | implemented |
| Recreation, pointer boundary, generation lifetime and shutdown | Runtime recovery/integration tests, readiness polling and shutdown verifier | Focused Sidecar tests and full Compose recreation/SIGTERM checkpoints | implemented |
| Stopped full database recovery | Verifier copies the stopped SQLite file set, injects invalid selected state only into its disposable volume, restores ownership and files | Failed startup, restored account/current/history and execution checkpoints | implemented |
| Deferred integrated checks and guidance | Dated README acceptance record and production recovery guide | Focused 70-test run; full Compose rerun; Step 4 recorded full Maven verify/package and image build | implemented |
| Release boundary | Snapshot dependency retained; README lists ordered pending gates | Git diff review; no publish, tag, or dependency switch | implemented |

## Active Project Guardrails

- The added fixture field and assertions are limited to the existing verifier; no new test platform or production abstraction was introduced.
- No application/test Java import from framework internals or autoconfigure was added. Existing public-API architecture coverage remains in the Sidecar suite.
- Admission, trusted identity, retained record, startup activation and the framework shutdown budget are unchanged; tests exercise their existing behavior.
- The checked dependency remains the developer-installed beta 5 snapshot. Release work remains pending.

## Open Questions and Assumptions

- None affecting local integrated acceptance. Final released-artifact and hosted CI checks remain separate release gates.

## Verification Results

- PASS — `python -m py_compile scripts/verify-production.py examples/quickstart/host/host.py` — both verifier and fixture parse.
- PASS — `.\mvnw.cmd -B -ntp '-Dtest=ProductionComposeBrowserIntegrationTest,RuntimeConfigurationRecoveryIntegrationTest,RuntimeConfigurationIntegrationTest,ManagementConfigurationHttpIntegrationTest,ExecutionConfigurationCorrelationIntegrationTest,RestGenerationIntegrationTest' test` — 70 tests, zero failures/errors, three expected Compose-only browser skips.
- PASS — `python scripts/verify-production.py --image loomspan-sidecar:sc5-local` — initial review baseline run passed; after the fix a second complete run passed all browser, mail, security, transfer, binding, recreation, stopped recovery, completion and cutoff checkpoints. The final SIGTERM observations were exit 143 in 2.25 seconds for admitted-work completion and 4.49 seconds for cutoff.
- PASS — `git diff --check` — no whitespace errors.
- NOT RUN — `.\mvnw.cmd -B -ntp verify` — Step 4 already recorded the full 260-test suite; review reran the focused affected suite and complete Compose verifier. Residual risk is limited to unrelated Sidecar tests not independently repeated in this review context.

## Residual Risks and Optional Developer Checks

No local acceptance blocker remains after the fix. Framework beta 5 publication, released-dependency switch, final Sidecar build/tests/commit, hosted CI and Sidecar release remain pending release gates outside this ticket. No optional configured-environment check is required for this local acceptance.

## Disposition

- `fixes-applied` — this context changed implementation artifacts, so a fresh independent Step 5 review is required.
