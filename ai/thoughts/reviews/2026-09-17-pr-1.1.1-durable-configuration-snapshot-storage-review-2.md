# Durable Configuration Snapshot Storage Code Review — Cycle 2

## Scope and Repository State

Reviewed the ticket, research, implementation and testing plans, design lens, all new storage production and test files, V1 migration, configuration, dependency and deployment changes, and connected mounted-runtime and architecture boundaries. Compared tracked changes with `HEAD` on `main` and inventoried staged, unstaged and untracked paths. No staged changes exist. The existing beta 5 pin and release/roadmap edits in `.github/workflows`, `AGENTS.md`, `ai/thoughts/design-lens.md`, the roadmap, `scripts/prepare-release.py`, and `ReleasePreparationTest.java` preceded this storage implementation; they are not reviewed as storage changes. The storage-related portion of the overlapping `pom.xml` and `README.md` changes is included.

The change introduces an authored-content value object, SQLite V1 schema, JDBC repository, transactional store, explicit migration/configuration, storage startup initialization, deployment volumes, documentation and tests. Existing mounted skill and REST route loading still supplies the running configuration. The ticket explicitly assigns activation and its startup integration to Phase 2.

## Findings

No actionable findings.

## Findings Resolved in This Context

None. No implementation artifacts were changed in this review context.

## Acceptance-Criteria and Plan Conformance

| Criterion/decision | Code evidence | Test evidence | Result |
| --- | --- | --- | --- |
| Recover complete exact named YAML and REST content, including empty content, placeholders and literal values; enforce labels and nulls | `ManagedConfiguration`, ordered document rows and raw REST text in `ConfigurationSnapshotRepository`; V1 constraints | Repository round trip and validation tests; store initial-empty test | implemented |
| Fresh local identity, separate source provenance, immutable content and status-only updates | UUID allocation and sequence in repository; copied immutable list; separate status table and content-update triggers | Repeated submit, source-ID, mutation, status and SQL-trigger assertions | implemented |
| Logical format 1 contract and included/excluded content | `README.md` durable snapshot section | `ConfigurationReferenceTest` covers configuration reference; contract text reviewed directly | implemented |
| Fresh migration and atomic one-time initialization; corrupt initialized state fails | Explicit Flyway dependency before repository; store transaction and singleton pointer/state checks | Fresh/reopen, missing pointer/status, stray rows, empty existing DB and checksum failure tests | implemented |
| No partial snapshot/pointer after transaction failure; SQLite settings and contention | `TransactionTemplate`, SQLite PRAGMAs and verification | Forced pointer/document failures with table counts; PRAGMA, busy-timeout and reopen tests | implemented |
| Deployment permissions, single-instance policy and supported framework boundary | Dockerfile, Compose, Kubernetes and README; existing ArchUnit rules | Configuration reference and ArchUnit tests | implemented |

The plans' storage-specific decisions are represented by the four-table V1 schema, raw REST YAML, one application initializer and explicit migration runner. V1 is the only supported schema, so no upgrade fixture is required. Status transitions, current-pointer changes after initialization, runtime activation, pruning and transfer operations remain in the adjacent tickets or later phases.

## Active Project Guardrails

- Simplicity: one SQLite database, one V1 migration and small direct SQL repository; no compatibility reader or generic database layer.
- Framework authority: storage retains complete `SkillDocument` values without interpreting skill YAML; the existing ArchUnit rule passes and no internal/autoconfigure types are used.
- Execution, identity, shutdown and diagnostics: storage adds no alternate runtime authority or Java skill handler. Stored `pending` status does not claim activation.

## Open Questions and Assumptions

- None affecting ticket-scoped correctness. The planned Phase 2 framework activation and existing execution-test beta 5 API adjustment are outside this ticket per its Pipeline notes.

## Verification Results

- FAIL — `.\mvnw.cmd -B -ntp '-Dtest=ConfigurationSnapshotRepositoryTest,ConfigurationSnapshotStoreTest,ConfigurationReferenceTest,SupportedLoomspanApiArchitectureTest' test` — test compilation stops in existing `src/test/java/ai/loomspan/sidecar/execution/TestSkillInvocationHandoff.java:35`: its beta 5 `AdmittedSkillInvocation` implementation lacks `generationId()`.
- PASS — `.\mvnw.cmd -B -ntp '-Dtest=ConfigurationSnapshotRepositoryTest,ConfigurationSnapshotStoreTest,ConfigurationReferenceTest,SupportedLoomspanApiArchitectureTest' surefire:test` — 13 tests passed against real temporary SQLite files. The focused test `.class` files were newer than their source files; direct Surefire bypassed the unrelated test-compilation blocker.
- FAIL — `.\mvnw.cmd -B -ntp verify` — same unrelated test-compilation failure before test execution.

## Residual Risks and Optional Developer Checks

The normal Maven lifecycle and application startup remain unverified until the later Phase 2 integration updates the beta 5 execution test and runtime activation. The focused storage tests establish this ticket's persistence, failure, contention and configuration behavior, but do not establish framework activation or deployment readiness. On the intended volume, an operator may confirm UID/GID 10001 can write the database and WAL/SHM files and that the filesystem supplies reliable local locking.

## Disposition

- `clean`
