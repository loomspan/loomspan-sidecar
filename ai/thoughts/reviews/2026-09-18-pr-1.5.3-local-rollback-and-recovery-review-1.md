# Local Rollback and Recovery Code Review — Cycle 1

## Scope and Repository State

Reviewed the ticket, research, implementation and testing plans, design lens, and the complete ticket-scoped staged, unstaged, and untracked checkout on `main` at `1ba03ec`. The change spans rollback API and browser flow, runtime publication, tests, and stopped database recovery guidance. There were no staged changes or unrelated developer edits identified. This review made no implementation-artifact changes.

## Findings

No actionable findings.

## Findings Resolved in This Context

None.

## Acceptance-Criteria and Plan Conformance

| Criterion/decision | Code evidence | Test evidence | Result |
| --- | --- | --- | --- |
| Retained statuses become fresh, validated snapshots with unchanged source and provenance; missing sources fail safely | `ManagementConfigurationRollbackService.review/confirm`; `RuntimeConfigurationService.rollbackConfiguration` rereads under the publication gate and calls `publishCandidate`; immutable store content and normal pruning | HTTP failed/pruned source tests; runtime published/failed/pending/current matrix | Implemented |
| Editor/admin authorization, CSRF, stale source/grant/runtime review, accessible safe browser flow | Rollback security matcher, session admission and grant/runtime comparison; history controls use text nodes, status and focus | HTTP role/CSRF/staleness tests; history Playwright role, selection, stale, missing and disconnect tests | Implemented |
| Precutover safety, destructive cutover, serialization and disconnect | Rollback holds publication and transition gates; shared publication path stages before `clearAll`; admission runs before cutover | Runtime gate and failure tests; HTTP draft loss, fault and cancelled client tests | Implemented |
| Database-first failure accounting and truthful restart state | Shared `publishCandidate` revert, status and mutation-fault handling; current/history observation | Rollback HTTP status fault and runtime activation failure; existing import/publication regression tests exercise the shared path | Implemented |
| Full stopped database recovery and scope guidance | README stopped copy/restore commands and recovery page; test copies the database set, removes stale companions, restores a populated account and published content | `RuntimeConfigurationRecoveryIntegrationTest.stoppedFullDatabaseBackupRestoresPriorSelectionAndHistory` | Implemented |
| Related regression, framework boundary, and no expanded backup feature | No new online backup/job or framework internal imports; existing ArchUnit rule | Full Maven `verify`, including architecture and browser tests | Implemented |

## Active Project Guardrails

- The implementation uses the existing public `SkillReloader` preparation/publication surface, standard Spring APIs, and one local snapshot record with a fresh ID. The ArchUnit test passed and no framework internal/autoconfigure dependency was added.
- The destructive transition uses the existing database-first pointer and generation retirement path; it does not introduce a second publication authority, job store, compatibility shim, or new backup mechanism.
- The installed beta 5 snapshot remained the dependency for this review. Framework release and Sidecar release remain outside this ticket.

## Open Questions and Assumptions

None affecting this review.

## Verification Results

- PASS — `.\mvnw.cmd -B -ntp verify` — full build and test suite completed with `BUILD SUCCESS` on 2026-09-19.
- PASS — `git diff --check` — no whitespace errors; Git emitted only line-ending conversion notices.

## Residual Risks and Optional Developer Checks

The local stopped database test does not exercise a deployed Docker or Kubernetes volume and its operating-system ownership. Phase 6 retains those environment-specific deployment and release checks. No optional check blocks this ticket's local correctness evidence.

## Disposition

- `clean`
