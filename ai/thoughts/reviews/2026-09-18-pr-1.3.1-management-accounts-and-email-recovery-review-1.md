# PR 1.3.1 Management Accounts and Email Recovery Code Review — Cycle 1

## Scope and Repository State

Reviewed the ticket, research, implementation and testing plans, design lens, staged/unstaged state, and all ticket-scoped untracked production and test files. The comparison base is the current `main` HEAD (`959d7a90121e14df07e6c5fd09ed47ea6f9db84b`); no staged changes were present. The pre-existing roadmap edit and adjacent PR 1.3.2/1.3.3 tickets were excluded from the implementation judgment and preserved. The change adds Flyway V2 account/bootstrap/token state, Spring Security management sessions and forms, JDBC identity operations, SMTP delivery, configuration, tests and documentation.

I traced bootstrap reservation through token issuance and activation, invitation/retry, password change/reset, session version and idle checks, account alteration, ordered security chains, mail failure, and execution/JWT isolation beyond the changed hunks. I checked security/privacy, persistence, concurrency, lifecycle, integration, operational and documentation consequences before comparing with the plans.

## Findings

No actionable findings.

## Findings Resolved in This Context

None. No implementation artifact was changed.

## Acceptance-Criteria and Plan Conformance

| Criterion/decision | Code evidence | Test evidence | Result |
| --- | --- | --- | --- |
| Distinct durable account bootstrap, uniqueness and protected credentials | V2 schema, `ManagementAccountRepository`, `ManagementIdentityService` | `ManagementIdentityStoreTest` real SQLite reopen, race, uniqueness, token and hash inspection | Implemented |
| Setup credential, one initial admin, pending email activation and retry | `ManagementIdentityService.setup/redeem`, bootstrap row, configured mail | `ManagementIdentityStoreTest`, `ManagementMailIntegrationTest`, `ManagementMailHttpIntegrationTest` | Implemented |
| Admin invite, roles, enable/disable and last-admin guard | Account controller/service and conditional SQL alteration | SQLite concurrent alteration test and management HTTP authorization tests | Implemented |
| Password policy, attempts, CSRF, session invalidation and idle deadline | `ManagementPolicy`, `ManagementAttemptLimiter`, ordered security chain, `ManagementSessionGuard` | Policy/limiter, HTTP security, change and idle tests | Implemented |
| Purpose-bound expiring reset, generic forgot, sender and trusted link origin | Token digest and transaction path, `ManagementMailService`, fixed forgot responses | SQLite token tests and loopback SMTP/HTTP tests | Implemented |
| Missing/unavailable mail preserves login and execution | Mail availability checks and fixed errors; execution chain remains stateless | SMTP failure/retry, HTTP missing-mail, existing execution and security suites | Implemented |
| Minimal forms, session/account APIs and deployment reference | Pages/controller, README, application and example YAML | HTTP form/API and configuration reference tests | Implemented |
| Supported framework boundary | No added internal/autoconfigure Loomspan imports | `SupportedLoomspanApiArchitectureTest` | Implemented |

## Active Project Guardrails

- The implementation uses SQLite, Flyway, Spring JDBC/Security/Mail and no new framework SPI or internal Loomspan Java dependency.
- Account/bootstrap state remains separate from configuration snapshots; execution JWT and framework observability boundaries retain their own chains.
- The design stays within this ticket's identity scope; no editor, lease, publication or transfer feature was added.

## Open Questions and Assumptions

- None affecting correctness or review confidence. The selected Full profile remains appropriate for security, persistence and session behavior.

## Verification Results

- PASS — `.\mvnw.cmd -B -ntp '-Dtest=ManagementIdentityStoreTest,ManagementAttemptLimiterTest,ManagementHttpIntegrationTest,ManagementMailIntegrationTest,ManagementMailHttpIntegrationTest,ConsoleSecurityIntegrationTest,ConfigurationReferenceTest,SupportedLoomspanApiArchitectureTest' test` — 18 tests, no failures or errors. An initial unquoted PowerShell invocation failed at parsing before Maven started; this corrected invocation is the executed test command.
- PASS — `.\mvnw.cmd -B -ntp verify` — 148 tests, no failures or errors; package completed against the installed beta 5 snapshot.
- PASS — `git diff --check` — no whitespace errors (Git reported only local LF/CRLF conversion notices).

## Residual Risks and Optional Developer Checks

- Browser keyboard, paste and autofill presentation can be inspected manually; automated HTTP tests establish the form inputs and server behavior.
- Production SMTP/HTTPS credentials and delivery were intentionally not exercised; loopback SMTP tests cover the protocol path and failure/retry behavior.

## Disposition

- `clean`
