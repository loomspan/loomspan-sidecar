# PR 1.1.2 Snapshot Recovery and History Code Review — Cycle 4

## Scope and Repository State

Reviewed the ticket-scoped unstaged and untracked tree against `main` at `00ecd40`, including storage code and schema, properties, application wiring, integration-test fixture updates, the child-process fixture, README backup/recovery commands, ticket, research, and both plans. There were no staged changes. Prior review documents were not read. The beta 5 snapshot dependency and V1 migration remain as specified.

## Findings

No actionable findings.

## Findings Resolved in This Context

None. No implementation artifact was changed in this context.

## Acceptance-Criteria and Plan Conformance

| Criterion/decision | Code evidence | Test evidence | Result |
| --- | --- | --- | --- |
| Atomic guarded A-to-B submission and B-to-A failed reversion; independent status | `ConfigurationSnapshotStore.submit`, `revert`, and `updateStatus`; repository compare-and-switch SQL | `submitAndRevertAreGuardedAndAtomic`, `rollsBackIncompleteSnapshotAndInitialPointer`, `statusRecordingDoesNotChangeSelectionOrOlderPendingHistory` | implemented |
| Restart selects the committed pointer independent of status and preserves complete content | `initialize`, `current`, and `requireBySequence` | Committed pending B and reverted A reopen tests; forked-JVM uncommitted write is killed after candidate and pointer writes, then reopens at A | implemented |
| Damaged initialized selection fails without fallback or secret-bearing diagnostic | Selected loader wraps reconstruction failures with sequence and stopped-instance restore guidance; no cause chain | Missing status/document, pointer/state damage, migration-history tests | implemented |
| Positive configurable retention and protected transactional pruning | `SidecarSnapshotProperties`, `prune`, `snapshotIdsOldestFirst`, and startup bean invocation | Default/configured/invalid property tests; mixed-status, protected-over-target, later-release, cascade, and startup-order tests | implemented |
| Consistent stopped-instance backup and recovery | README stop/copy/restore commands include existing WAL/SHM members and replacement of stale companions | `stoppedDatabaseCopyRestoresCompleteCurrentAndHistory` compares selected content and retained identities/statuses after corruption and restore | implemented |
| Storage and runtime boundaries remain distinct | Store documentation and README label the pointer as intended production; no runtime coordinator is added | Storage tests assert selection and history only; README names Phase 2 checks | implemented |

## Active Project Guardrails

- Uses the existing V1 schema, Spring JDBC transactions, and direct repository operations without a compatibility bridge or new runtime registry.
- No new dependency on Loomspan internals or autoconfigure types; the ArchUnit check passed in the full suite.
- No shutdown hook or extra drain budget was introduced.
- Selected-content failure messages omit YAML and exception causes. The README warns that full database backups include literal sensitive values and have no v1 encryption.
- Local beta 5 snapshot was used; no framework rebuild, repository, release, or tag was introduced.

## Open Questions and Assumptions

None affecting this storage unit. Phase 2 supplies runtime protected IDs, completed-update pruning, publication fault recovery, and dispatch gating as the ticket specifies.

## Verification Results

- FAIL — `.\mvnw.cmd -B -ntp -Dtest=ConfigurationSnapshotStoreTest,ConfigurationSnapshotRepositoryTest,SidecarDefaultsTest,ConfigurationReferenceTest test` — PowerShell parsed the unquoted comma-separated property as a parameter list; Maven did not run.
- PASS — `.\mvnw.cmd -B -ntp '-Dtest=ConfigurationSnapshotStoreTest,ConfigurationSnapshotRepositoryTest,SidecarDefaultsTest,ConfigurationReferenceTest' test` — 22 tests passed, including real SQLite interruption, rollback, pruning, and backup/restore.
- PASS — `.\mvnw.cmd -B -ntp verify` — 121 tests passed, no failures/errors/skips; packaged the application.

## Residual Risks and Optional Developer Checks

The README's stop/copy/restore commands were inspected against the Compose and Kubernetes samples but were not run against a mounted deployment volume. An operator can rehearse them on a disposable local persistent volume. Phase 2 runtime integration checks remain required before deployment/release; this storage review does not establish runtime publication or retirement behavior.

## Disposition

`clean`
