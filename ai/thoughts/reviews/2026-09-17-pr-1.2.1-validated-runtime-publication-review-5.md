# Validated Runtime Publication Code Review — Cycle 5

## Scope and Repository State

Reviewed the ticket-scoped working tree on `main` against the untracked ticket, research, implementation plan, and testing plan. Scope included staged (none), unstaged production, test, script, configuration, example, and README edits, and untracked runtime service, generation registry, tests, and controlled empty skill source. The review traced startup selection through framework publication and execution admission; exact draft validation and submission through database and framework faults; generation ownership through REST invocation, retirement, pruning, and shutdown; and retained execution correlation. No prior review document was used.

## Findings

No actionable findings.

## Findings Resolved in This Context

None. No implementation artifact was changed.

## Acceptance-Criteria and Plan Conformance

| Criterion/decision | Code evidence | Test evidence | Result |
| --- | --- | --- | --- |
| Database-selected startup, empty baseline, current discovery and checks | `RuntimeConfigurationService.run`, `application.yml`, `ExecutionCoordinator.openAfterActivation`, both API controllers | `RuntimeConfigurationIntegrationTest`, mounted-skill and authenticated API regressions | Implemented |
| Exact validation and fresh serialized publication | `RuntimeConfigurationService.validate/publish`, Phase 1 `ConfigurationDraft` identity check | Runtime integration validation and competing-publication tests | Implemented |
| URL allowlist and authored reference preservation | `RestRouteLoader.resolveBaseUrl`, `RestRoutesProperties`, immutable stored configuration | `RestRouteLoaderTest`, stored-text assertion | Implemented; transfer round trip belongs to Phase 5 |
| Matching REST generations and cleanup | `GenerationRestResources`, fixed `GenericRestSkillHandler`, immediate HTTP client close | `RestGenerationIntegrationTest`, `RestHandlerLifecycleIntegrationTest` | Implemented |
| Publication fault states | Ordered submit, publish, revert, status and mutation gate in runtime service | Runtime fault matrix and recovery integration | Implemented |
| Restart and stopped backup recovery | Selected pointer activation regardless of status | `RuntimeConfigurationRecoveryIntegrationTest` | Implemented |
| Captured execution snapshot UUID | `ExecutionCoordinator.handoff`, `ExecutionSnapshot`, GET response | Correlation integration, coordinator and API tests | Implemented |
| Pruning protection | Publication lock and generation registry IDs supplied to `store.prune` | Protected overflow and later-prune integration | Implemented |
| Documentation, examples, regressions and API boundary | README, compose/Kubernetes, verification scripts; public `ai.loomspan.api` use | Full 135-test suite, including ArchUnit | Implemented |

Phase 3 remains responsible for real session, lease, account, and protected management admission. Phase 5 owns transfer and destructive cutover. The current internal service exposes no management endpoint or unchecked public publication option.

## Active Project Guardrails

- The implementation uses public Loomspan API types; the full suite includes the application and test ArchUnit boundary checks. No Sidecar Java skill method or replacement framework bean was introduced.
- Framework preparation and invocation remain authoritative. Sidecar uses one worker handoff owner and retains correlation in the existing execution record.
- The framework shutdown lifecycle has phase `Integer.MAX_VALUE`; Sidecar's REST resource lifecycle uses phase `0`, so client cleanup follows framework completion or cutoff. Existing JWT and observer paths remain covered by regressions.
- Mounted skill and route sources were replaced directly with a controlled empty framework source and database-authored content, consistent with the ticket's replacement of the earlier startup-only design.

## Open Questions and Assumptions

None affecting this review disposition.

## Verification Results

- PASS — `mvn -q '-Dtest=RuntimeConfigurationIntegrationTest,RuntimeConfigurationRecoveryIntegrationTest,RestGenerationIntegrationTest,ExecutionConfigurationCorrelationIntegrationTest,RestHandlerLifecycleIntegrationTest,ExecutionShutdownIntegrationTest,SupportedLoomspanApiArchitectureTest' test` — exit 0; real temporary SQLite, installed framework snapshot, loopback servers, shutdown, and public-boundary checks.
- PASS — `mvn test` — exit 0; 135 tests, zero failures, errors, or skips.
- PASS — `python scripts/verify-image.py --help` — exit 0.
- PASS — `git diff --check` — exit 0; only Windows line-ending notices.
- FAIL — `mvn -q -Dtest=RuntimeConfigurationIntegrationTest,RuntimeConfigurationRecoveryIntegrationTest,RestGenerationIntegrationTest,ExecutionConfigurationCorrelationIntegrationTest,RestHandlerLifecycleIntegrationTest,ExecutionShutdownIntegrationTest,SupportedLoomspanApiArchitectureTest test` — PowerShell rejected the unquoted comma-separated argument before Maven started. The quoted invocation above passed.

## Residual Risks and Optional Developer Checks

Docker image and Kubernetes checks require a configured isolated local environment and were not run. Hosted CI and release artifact validation remain deferred under the ticket's framework publication policy.

## Disposition

- `clean`
